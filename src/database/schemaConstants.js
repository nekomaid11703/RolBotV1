/**
 * Constantes de versión de esquema de base de datos.
 * Fuente única de verdad para la versión del schema. Utilizada por
 * schemaVersion.js y schemaMigration.js para evitar desajustes.
 * @constant CURRENT_VERSION
 * @type {string}
 */
const CURRENT_VERSION = "2.3.0";

module.exports = { CURRENT_VERSION };
