/**
 * @constant MAX_INVENTORY_SIZE
 * @type {number}
 */
const MAX_INVENTORY_SIZE = 20;
/**
 * @constant MAX_STACK_SIZE
 * @type {number}
 */
const MAX_STACK_SIZE = 99;

module.exports = {
  MAX_INVENTORY_SIZE,
  MAX_STACK_SIZE,
};
